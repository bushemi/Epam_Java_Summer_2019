image: maven:latest

variables:
  MAVEN_CLI_OPTS: "-s .m2/settings.xml --batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build
  script:
    - cd ./homework_11_CI_CD/
    - mvn $MAVEN_CLI_OPTS compile

test:
  stage: test
  script:
    - cd ./homework_11_CI_CD/
    - mvn $MAVEN_CLI_OPTS test

deploy:
  stage: deploy
  script:
    - cd ./homework_11_CI_CD/
    - mvn $MAVEN_CLI_OPTS deploy -e
